/**
 * Sunset stripe — a faixa-assinatura da marca (DESIGN.md sunset-stripe-band).
 * Aparece no pé de toda página, logo acima do rodapé.
 */
export function SunsetStripe() {
  return <div className="sunset-stripe h-2.5 w-full" aria-hidden="true" />;
}
